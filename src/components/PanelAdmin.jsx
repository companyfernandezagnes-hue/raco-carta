import { useState, useEffect, useRef } from 'react'import { supabaseAdmin } from '../lib/supabaseAdmin'const PASS_HASH = 'Y21GamJ6SXdNalU9'// v3 - usando Google Gemini APIfunction verificarPassword(input) {  try { return btoa(btoa(input)) === PASS_HASH } catch { return false }}const CAMPOS_IA = [  'nombre','categoria','subcategoria','descripcion','bodega','productor',  'pais','region','anada','uvas','tipo_uva_secundaria','parcela',  'nota_cata','maridajes','temperatura','graduacion',  'precio_copa','precio_botella','notas_ia']async function rellenarConIA({ nombre, fotoBase64, apiKey, setForm, setIaLoading, setIaError }) {  setIaLoading(true)  setIaError('')  try {    const parts = []    const systemPrompt = `Eres un experto en vinos y bebidas de restaurante. Dado el nombre o la foto de una bebida, rellenas una ficha completa en JSON con estos campos exactos:nombre, categoria (Vino/Cerveza/Coctel/Refresco/Agua/Cafe/Destilado/Otro), subcategoria, descripcion, bodega, productor, pais, region (denominacion de origen), anada (año numero o null), uvas (uva principal), tipo_uva_secundaria, parcela, nota_cata, maridajes (array de strings), temperatura, graduacion (numero o null), precio_copa (numero o null), precio_botella (numero o null), notas_ia.IMPORTANTE: Solo rellena con datos reales y conocidos. Si no sabes un campo, pon null. No inventes datos. Devuelve SOLO el JSON, sin texto extra.`    parts.push({ text: systemPrompt })    if (fotoBase64) {      const base64Data = fotoBase64.split(',')[1]      const mimeType = fotoBase64.split(';')[0].split(':')[1]      parts.push({ inline_data: { mime_type: mimeType, data: base64Data } })      parts.push({ text: 'Analiza esta bebida y rellena la ficha completa en JSON.' })    } else {      parts.push({ text: `Rellena la ficha completa en JSON para esta bebida: ${nombre}` })    }    const res = await fetch(      `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`,
